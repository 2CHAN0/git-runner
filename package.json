{
  "name": "git-runner-ci-test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:integration": "vitest --config vitest.integration.config.mts",
    "test:integration:setup": "docker compose -f docker-compose.test.yml up -d --wait && DATABASE_URL=postgresql://test:test@localhost:5433/test DIRECT_URL=postgresql://test:test@localhost:5433/test npx prisma db push --skip-generate",
    "test:integration:teardown": "docker compose -f docker-compose.test.yml down",
    "build": "tsc --noEmit && echo 'Build check passed'"
  },
  "devDependencies": {
    "@prisma/client": "^6.6.0",
    "prisma": "^6.6.0",
    "typescript": "^5.7.0",
    "vitest": "^3.1.0"
  }
}
